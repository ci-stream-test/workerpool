
version: v2.0
name: "build and test"
 
on:
  mr: 
    target-branches:
      - master
 
stages:
  - name: "mr"
    jobs: 
      job:
        steps: 
          - name: "checkout"
            checkout: self

          - name: "build" 
            run: |-
              #!/bin/bash
              go build ./...
          
          - name: "test"
            run: |-
              #!/bin/bash
              go test ./...
          